const socialState = {
  github: {
    url: 'https://github.com/Klumhru',
  },
}

export const social = (state = socialState, action) => {
  switch (action.type) {
    default: {
      return state
    }
  }
}
